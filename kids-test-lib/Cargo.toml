[package]
name = "kids-test-lib"
description = "Test utilities for Keycloak Identity Syncer — provides helper types like RandomId and common constants used across integration and E2E tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rand = "0.9.0"

[lints]
workspace = true
